---
title: 总览 — 战斗脚本架构与目录拓扑
tags: [higame, battle, gas, unlua, overview]
updated: 2026-05-11
---

# 总览 — 战斗脚本架构与目录拓扑

HiGame 的战斗系统是 **Epic GAS(GameplayAbilitySystem) + 自研 DDS(分布式 Dedicate Server) + UnLua 业务层**的三明治结构。C++ 仅提供骨架,绝大部分技能逻辑、配置读取、跨系统协同、伤害结算都由 Lua 实现并热更。本页画出整个战斗子系统的边界、目录结构、启动链路,后续每一页深挖其中一块。

## 三层 Lua 代码分隔与战斗子树

HiGame 的 Lua 代码在 `Content/Script/` 下被强制分成三层,**战斗代码主要分布在 Common + Server,Client 仅写驱动与表现**[^c06]。

```mermaid
flowchart TB
    subgraph SCRIPT["Content/Script/"]
        direction TB
        COMMON["CommonScript/<br/>双端共用<br/>(★ skill/ability/, skill/ExecCalc/,<br/>actors/components/skill_*, buff_*, calc_*)"]
        CLIENT["ClientScript/<br/>客户端专用<br/>(skill/SkillDriver, skill/SkillManagerBase,<br/>battle_state_component, monster/ai_*)"]
        SERVER["ServerScript/<br/>服务端专用<br/>(ai/BTTask/BTTask_HI_TryActiveAbility,<br/>actors/monsters/*, skill/ability/* 服务器分支)"]
        SKILLOLD["skill/<br/>★ 旧版战斗子树(部分仍在用)<br/>(Affix, knock, GameplayCues,<br/>GameplayEffect/Buff, GameplayEffectComponent)"]
    end

    COMMON -. 主战斗逻辑 .-> X1["GA / GE / ExecCalc / Buff / Passive / Knock"]
    CLIENT -. 输入与表现 .-> X2["SkillDriver / SkillObj / Cue / 飘字"]
    SERVER -. 服务端授权 .-> X3["BTTask / 怪物 GA Server 覆写"]
    SKILLOLD -. 历史遗留 .-> X4["少量 Affix/Knock 实现仍在 skill/ 下"]

    style COMMON fill:#4a9eff,color:#fff
    style SKILLOLD fill:#ff9f43,color:#fff
    style X4 fill:#ffc107,color:#000
```

> **路径陷阱**:历史上有两份 `skill/` 树。**新代码一律落 `Content/Script/CommonScript/skill/ability/`**,旧的 `Content/Script/skill/`(顶层无 CommonScript 前缀)只剩 Affix 词缀、Knock、SimpleSummon、GameplayEffectComponent 等局部模块还在用。读老代码时注意区分,改造时优先迁到 CommonScript。

### 战斗 Lua 子树全图

```mermaid
flowchart TD
    ROOT["Content/Script/"]
    ROOT --> CS["CommonScript/skill/<br/>★ 主战斗逻辑"]
    ROOT --> SS["skill/<br/>(旧, 部分模块仍在用)"]
    ROOT --> CSA["CommonScript/actors/components/<br/>组件层 (BuffComponent / CalcComponent /<br/>SkillComponent / PassiveSkillComponent /<br/>BattleStateCommon)"]
    ROOT --> CL["ClientScript/skill/<br/>SkillDriver / SkillManagerBase / SkillObj"]
    ROOT --> SR["ServerScript/ai/BTTask/<br/>BTTask_HI_TryActiveAbility 等"]

    CS --> AB["ability/<br/>★ 21 个分支"]
    CS --> EC["ExecCalc/<br/>ExecCalc_Base, ExecCalc_Damage"]
    CS --> MNS["monster/<br/>GAMonsterExecute"]

    AB --> ABA["GABase / GASkillBase /<br/>GAPlayerBase / GASuperSkill /<br/>GAMonsterRush / GAWeaponBase"]
    AB --> KN["Knock/<br/>玩家被击 + 怪物被击"]
    AB --> AF["Affix/<br/>BingBao/Cleaner/DianRen/<br/>DuZhao/FuLian/LeiJI/RanShao/ShouHu"]
    AB --> PA["passiveability/<br/>GAPassiveAbilityBase / actualeffect /<br/>roguelike_rune"]
    AB --> RG["roguelike/<br/>Arabella, base, thunderspear"]
    AB --> SH["SwapHead/<br/>头部替换玩法"]
    AB --> RM["RMS/<br/>怪物 RMS 跳跃等"]
    AB --> LT["luatask/<br/>LuaAbilityTaskFactory + Task 子类"]
    AB --> MMC["MMC/<br/>ModifyMagnitudeCalc Lua 实现"]

    SS --> SSA["skill/ability/<br/>(GAAimBase, GACapture, GACharge,<br/>GAMoon, GASummons, GAThrow…)"]
    SS --> SSK["skill/knock/<br/>GAKnockBackAvatar/Monster/Vehicle…"]
    SS --> SSGE["skill/GameplayEffect/Buff/Affix/<br/>LiuXue/MMC_Buff_LiuXue.lua"]
    SS --> SSGEC["skill/GameplayEffectComponent/<br/>GEComp_ApplyChance / GEComp_Shield…"]
    SS --> SSGC["skill/GameplayCues/<br/>GC_SetMontagePlayRate / gc_sub_camerashake /<br/>gc_sub_sequence"]

    style CS fill:#4a9eff,color:#fff
    style AB fill:#51cf66,color:#fff
    style EC fill:#ff6b6b,color:#fff
```

### 关键三层协同(运行时)

```mermaid
sequenceDiagram
    participant Input as ClientScript<br/>SkillDriver
    participant ASC as C++<br/>UHiAbilitySystemComponent
    participant GA as Lua<br/>GASkillBase 实例
    participant Anim as 动画<br/>AnimNotify
    participant Calc as Lua<br/>ExecCalc_Damage
    participant Cue as Lua<br/>GameplayCue
    participant BT as Server<br/>BTTask 怪物 AI

    Input->>ASC: BP_TryActivateAbilityByHandle
    ASC->>GA: K2_ActivateAbility (反射调用 Lua)
    GA->>GA: HandleActivateAbility<br/>OnActivateAbility / HandlePlayMontage / HandleCalc
    Anim->>GA: AnimNotify_GameplayEvent<br/>EventTag = Calc.SwordSlash
    GA->>GA: OnCalcEvent → MakeSpecsByTag → InitCalcForHits
    GA->>Calc: ApplyEffectContainerSpec (TargetData)
    Calc->>Calc: Execute (FExecDamageCalcData 一次性预取 26 属性)
    Calc->>ASC: SetByCaller.Damage 输出
    ASC->>Cue: HandleGameplayCue (RemoveStack / Burst)
    BT->>ASC: BTTask_HI_TryActiveAbility (服务器路径)
```

> 这条链是**所有战斗代码读懂后都能 5 秒画出来的核心链路**。任何一个新技能本质都是在这条链路上挂载自己的 Tag、自己的 GE、自己的 Cue 资源。详见 [3. EffectContainer 与 Tag 驱动结算流](3.%20EffectContainer%20与%20Tag%20驱动结算流.md)。

## C++ 战斗类层次

战斗的 C++ 入口集中在两处目录[^c01][^c02]:

```mermaid
classDiagram
    class UDDSAbilitySystemComponent {
        <<UE/DDS Plugin>>
    }
    class UHiAbilitySystemComponent {
        +InitAttributes(Magnitudes)
        +ApplyAttributeModifyEffects()
        +MakeBuffEffectSpec(BuffID, Level)
        +ApplyBuffToSelf / ApplyBuffToTarget
        +GiveAbilityWithParams(FHiAbilityParam)
        +PauseGameplayEffectDuration()
        +SetGameplayEffectDurationHandle()
    }
    UDDSAbilitySystemComponent <|-- UHiAbilitySystemComponent

    class UGameplayAbility {
        <<UE GAS>>
    }
    class UHiGameplayAbility {
        +EffectContainerMap (Tag → Container)
        +CooldownDuration / CooldownTags
        +CostGameplayEffectDef
        +AbilityData (TArray<UHiAbilityDataBase>)
        +MagnitudeModifiers (Tag → Modifier)
        +K2_OnGiveAbility / K2_OnAvatarSet
        +K2_ActivateAbilityParams
        +K2_OnCompleted / K2_OnInterrupted
        +PreTransfer / PostTransfer (DDS)
        +MakeEffectContainerSpecByTag()
        +ApplyEffectContainerSpec()
    }
    UGameplayAbility <|-- UHiGameplayAbility
    UGameplayAbility <|-- UHiPassiveGameplayAbility

    class UHiPassiveGameplayAbility {
        +TriggerAbilityInfo
        +AutoEffectInfoList (List)
        +EffectInfoToSelfList / EffectInfoToTargetList
        +SingleTriggerData (含 CD / 概率)
        +ActivatePassiveAbility / EndPassiveAbility
    }

    class UAttributeSet {
        <<UE GAS>>
    }
    class UHiAttributeSet {
        +ATTRIBUTE_ACCESSORS
        +OnPreGameplayEffectExecute (BlueprintNativeEvent)
        +OnPostGameplayEffectExecute (BlueprintNativeEvent)
        +OnPreAttributeChange
        +InitEffectListeners
    }
    UAttributeSet <|-- UHiAttributeSet
    UHiAttributeSet <|-- UHiHealthAttributeSet : (派生)

    class UGameplayEffect {
        <<UE GAS>>
    }
    class UHiSelfGameplayEffect {
        +bApplyToAllTeamMembers
    }
    class UHiTeamBuffGEBase {
        +bTeamBuff
    }
    UGameplayEffect <|-- UHiSelfGameplayEffect
    UGameplayEffect <|-- UHiTeamBuffGEBase

    class AGameplayAbilityTargetActor {
        <<UE GAS>>
    }
    class AHiTargetActorBase {
        +ProjectileClass
        +GameplayEffectsHandle
        +SelfGameplayEffectsHandle
        +KnockInfo (Replicated)
        +Spec : FHiTargetActorSpec
        +OnTick / OnStartTargeting / OnTargetDataReceived
    }
    AGameplayAbilityTargetActor <|-- AHiTargetActorBase

    class AHiProjectileActorBase {
        +CapsuleComponent
        +GameplayEffectsHandle
        +SelfGameplayEffectsHandle
        +KnockInfo
    }
```

### 各模块职责

| 模块 | 关键文件 | 说明 |
|------|---------|------|
| **HiAbilitySystemComponent** | `Public/Component/HiAbilitySystemComponent.h` | DDS 增强版 ASC,补:`InitAttributes`/`ApplyAttributeModifyEffects`/`MakeBuffEffectSpec`/`ApplyBuffToSelf`/`PauseGameplayEffectDuration`,以及 `bClientReady` 同步标志 |
| **HiAttributeComponent** | `Public/Component/HiAttributeComponent.h` | 战斗属性中间层,持有 `FHiHeroAttributeContainer` + `FHiHeroGameplayEffectContainer`,**用 UniqueID 解决 RPC 时序**;`Client_ExecuteAttributeModifies` 携带 TargetUniqueID |
| **HiGameplayAbility** | `Public/HiAbilities/HiGameplayAbility.h` | 战斗 GA 基类,提供 `EffectContainerMap`/`AbilityData[]`/`MagnitudeModifiers`/`PreTransfer/PostTransfer`/`MakeEffectContainerSpecByTag*`;Lua 子类继承蓝图后再继承 |
| **HiPassiveGameplayAbility** | `Public/HiAbilities/HiPassiveGameplayAbility.h` | 被动技能基类,有 `TriggerAbilityInfo`/`AutoEffectInfoList`/`SingleTriggerData`(冷却+概率) |
| **HiAttributeSet** | `Public/Attributies/HiAttributeSet.h` | 所有自定义属性集的基类,提供 `OnPre/PostGameplayEffectExecute` BlueprintNativeEvent 给蓝图层定制 clamp/listener |
| **HiTargetActorBase** | `Public/HiAbilities/HiTargetActorBase.h` | TargetActor 基类(自定义 reticle / sweep 区域 / 抛射物模板) |
| **HiProjectileActorBase** | `Public/HiAbilities/HiProjectileActorBase.h` | 投射物基类(胶囊体 + GE Spec + KnockInfo) |
| **HiBuffComponent** | `Public/Component/HiBuffComponent.h` | Buff 组件(`AddBuffByID` 蓝图实现) |
| **HiAbilityTask_PlayMontage / PlaySequence** | `Public/HiAbilities/Tasks/*` | 自研 PlayMontage/PlaySequence Task,扩展了同步与位面迁移恢复 |
| **HiGameplayCueManager** | `Public/HiAbilities/HiGameplayCueManager.h` | Cue 管理器,关闭了启动期 AsyncLoad,改为按需异步 |

详细 Lua / C++ 边界 → [11. C++ 与 Lua 边界 + DDS 迁移](11.%20C%2B%2B%20与%20Lua%20边界%20+%20DDS%20迁移.md)。

## Lua 战斗骨架(GA 主线)

战斗 Lua 的根基是 **`GABase` → `GASkillBase` → `GAPlayerBase / GAMonsterRush / GASuperSkill / GAWeaponBase / GAWithStand / GASequence / GAKnockBase / GAPassiveAbilityBase`**[^c06]。

```mermaid
flowchart TB
    GABase["GABase<br/>(skill/ability/GABase.lua)<br/>K2_OnGiveAbility / K2_OnAvatarSet /<br/>K2_OnEndAbility / OnComboTailEvent / Tick"]
    GABase --> GASkillBase["GASkillBase<br/>★ 普通主动技能<br/>K2_ActivateAbility 入口<br/>HandleActivateAbility<br/>OnCalcEvent / OnApplyToSelfCalc /<br/>OnApplyToTargetCalc / __OnCalcByType"]
    GABase --> GAPassive["GAPassiveAbilityBase<br/>(passiveability/Base/)<br/>OnInitParameter / ActivatePassiveAbility /<br/>OnInitEventTrigger / HandleAutoEffect*"]
    GABase --> GAKnock["GAKnockBase<br/>(skill/knock/)<br/>K2_ActivateAbilityFromEvent /<br/>HandleSwitchStateTagInKnock /<br/>OnInheritComboTailAttackable"]

    GASkillBase --> GAPlayerBase["GAPlayerBase<br/>(ClientScript/skill/ability/)"]
    GASkillBase --> GAMonsterRush["GAMonsterRush"]
    GASkillBase --> GASuperSkill["GASuperSkill<br/>必杀技"]
    GASkillBase --> GAWeaponBase["GAWeaponBase"]
    GASkillBase --> GASequence["GASequence<br/>过场动画"]
    GASkillBase --> GAWithStand["GAWithStand<br/>霸体"]

    GAPlayerBase --> Concrete1["GAYunshuoSkill<br/>(玩家-云朔)"]
    GAMonsterRush --> Concrete2["GA_Monster_*<br/>(各种怪物)"]

    style GABase fill:#1a1a2e,color:#fff
    style GASkillBase fill:#4a9eff,color:#fff
    style GAPassive fill:#ff6b6b,color:#fff
    style GAKnock fill:#51cf66,color:#fff
```

> 这 8 大基类各管一类玩法,新写技能时第一件事就是**选对基类**:
> - 玩家主动 → `GAPlayerBase` 派生
> - 必杀 → `GASuperSkill` 派生
> - 怪物冲锋 → `GAMonsterRush` 派生
> - 普攻被击 → `GAKnockBase` 派生(注意 `K2_ActivateAbilityFromEvent` 而非 `K2_ActivateAbility`)
> - 被动 → `GAPassiveAbilityBase` 派生(走 `OnInitEventTrigger` 注册事件,而非走 montage)
> - 词缀(可叠加属性 buff) → `GA_AffixBase` 派生
> - 过场 → `GASequence` 派生

详见 [2. GA 继承层次与生命周期](2.%20GA%20继承层次与生命周期.md)。

## 战斗启动链路 — 从角色登场到第一刀

```mermaid
sequenceDiagram
    participant World as Game World
    participant PS as PlayerState
    participant Char as HiCharacter (Avatar)
    participant SC as SkillComponent (Lua)
    participant ASC as UHiAbilitySystemComponent
    participant Attr as UHiAttributeComponent
    participant SD as SkillDriver (Client)

    World->>Char: PossessedBy / OnRep_PlayerState
    Char->>SC: InitGameAbilitySystem
    SC->>ASC: InitAbilityActorInfo (Owner, Avatar)
    SC->>Attr: InitializeWithAbilitySystem
    Attr->>Attr: InitAttributeListener<br/>(注册 AttributesToListenFor 回调)
    SC->>SC: AddCommonAttributeSet<br/>(从 PlayerState.AttributeSets 继承)
    SC->>SC: PlayerApplyInitAttrMagnitudes<br/>(给玩家走 InitHeroAttributes 链路)
    SC->>ASC: GiveAbilityWithParams (FHiAbilityParam[])<br/>(GA + Level + InputID + UserData)
    ASC->>Char: OnGiveAbility 回调 → BP_OnGiveAbility → Lua GABase:K2_OnGiveAbility
    SC->>SD: SkillDriver.new (新建驱动器)
    SD->>SD: OnRepNewSkill (按 SkillType 建 SkillManager)
    Note over SC,SD: 至此 ASC 已就绪,等待输入或 BT 触发
```

详见 [4. AttributeSet 与 Hero 属性中间层](4.%20AttributeSet%20与%20Hero%20属性中间层.md) 与 [10. 输入、AI 与动画接入](10.%20输入、AI%20与动画接入.md)。

## 战斗触发与战斗状态(Battle State)

战斗系统不止"按一下技能",还分**进入战斗 / 战斗中 / 离开战斗**三个阶段[^c12]:

```mermaid
stateDiagram-v2
    [*] --> NotInBattle
    NotInBattle --> InBattle : OnEnterBattle<br/>(BattleTargetComponent)
    InBattle --> InBattle : 受击/释放/扣血/给 Buff<br/>BattleStateCommon 记录数据
    InBattle --> NotInBattle : OnLeaveBattle<br/>(超时无目标 / 玩家死亡 / 怪物全清)

    note right of InBattle
        BattleStateCommon 记录:
        - extreme_dodge_cnt / success
        - counter_cnt / success
        - out_balance_cnt
        - hero_up_time
        - 战伤 (BattleDamageRecord)
    end note
```

`PlayerState.BattleTargetComponent:IsInBattle()` 是全局判定入口。`SendMessage("OnEnterBattle/OnLeaveBattle")` 由 BattleTargetComponent(C++/蓝图)在感知到敌对目标后广播,Lua 端 `BattleStateCommon` 收消息后开关数据记录。

具体怪物布设是另一个独立子系统:
- **Trigger 体积** — `Public/Trigger/HiTriggerBox.h` / `HiTriggerVolume.h` / `ConvexTriggerVolume_POI.h` — 玩家进入触发器后才允许 Spawn 怪物波次
- **TriggerManagementSystem** — `Public/TriggerManagementSystem/` — 提供物理材质/体积/全局/Voxel 触发,并能挂 BGM/Environment Override 等 Effect 组件
- **GameFeatureAction_AddSpawnedActors** — `Public/GameFeatures/GameFeatureAction_AddSpawnedActors.h` — Game Feature 启用时自动塞 Actor 到关卡

详见 [12. 进阶 Cookbook 与常见陷阱](12.%20进阶%20Cookbook%20与常见陷阱.md) 的"战斗触发与怪物布设"章节。

## 三大入口:输入 / AI / 动画

任何技能最终都是被以下三种途径之一激活的[^c11]:

```mermaid
flowchart LR
    INPUT["① 输入<br/>(玩家)"]
    AI["② AI<br/>(怪物 / 助战)"]
    ANIM["③ 动画事件<br/>(Knock 被击 / 链式技)"]

    INPUT --> SKD["ClientScript/skill/SkillDriver<br/>+ SkillManagerBase"]
    SKD --> ASCT["ASC.BP_TryActivateAbilityByHandle"]

    AI --> BT["ServerScript/ai/BTTask/<br/>BTTask_HI_TryActiveAbility"]
    BT --> ASCT

    ANIM --> AN["AnimNotify_GameplayEvent /<br/>UHiAnimNotifyState_*"]
    AN --> ASCT2["ASC.HandleGameplayEvent<br/>(GA 内 WaitGameplayEvent 监听)"]

    ASCT --> GA["UHiGameplayAbility.<br/>ActivateAbility"]
    ASCT2 --> GA

    style INPUT fill:#4a9eff,color:#fff
    style AI fill:#ff9f43,color:#fff
    style ANIM fill:#51cf66,color:#fff
```

详见 [10. 输入、AI 与动画接入](10.%20输入、AI%20与动画接入.md)。

## 一句话总结

> **HiGame 战斗 = GAS + DDS 中间层 + Lua 业务**。
> C++ 提供 **ASC / AttributeComponent / GA 基类 / TargetActor / Projectile / Cue Notify** 的最小骨架;
> Lua 在 **GABase 上面盖 8 大主线 GA**(Skill/Player/Monster/Super/Knock/Passive/Affix/Sequence),
> 通过 **EffectContainerMap + Tag 驱动**把 AnimNotify、伤害结算、Buff 应用、表现 Cue 串成一条流水线。
> 任何新技能都是 **选基类 + 配 Tag + 配 EffectContainer + 写 Lua 钩子 + 配 Cue 资源**这五件事的组合。

[^c01]: `Source/HiGame/Public/HiAbilities/HiGameplayAbility.h` `HiGameplayEffect.h` `HiAbilityTypes.h`
[^c02]: `Source/HiGame/Public/Component/HiAbilitySystemComponent.h` `HiAttributeComponent.h` `HiBuffComponent.h`,`Public/Attributies/HiAttributeSet.h`
[^c06]: `Content/Script/CommonScript/skill/ability/GABase.lua` `GASkillBase.lua` `GAPlayerBase.lua`
[^c11]: `Content/Script/ClientScript/skill/SkillDriver.lua`、`Content/Script/ServerScript/ai/BTTask/BTTask_HI_TryActiveAbility.lua`
[^c12]: `Content/Script/CommonScript/actors/components/battle_state_common.lua` `battle_starup_component.lua`,`Source/HiGame/Public/Trigger/*` `TriggerManagementSystem/*`
