---
title: UIInfo 配置与 DataTable 注册
tags: [higame, ui, ui-info, datatable, registration]
updated: 2026-05-10
---

# UIInfo 配置与 DataTable 注册

每个 UI 必须先在 UE DataTable `/Game/CP0032305_GH/Blueprints/DT/UIInfo` 中注册一行,运行时通过 `GH_FunctionLibrary.GetUIInfoContent` 读取并由 `ui/uiframework/ui_define.lua:InitUIDef()` 组装成 `UIDef.UIInfo["UI_xxx"]` 表[^49]。本页给出**所有可配字段的完整说明**,以及典型场景的推荐配置。

## 注册流程总图

```mermaid
flowchart LR
    EDIT["策划 / 程序<br/>在 UE Editor 编辑"]
    DT["DataTable<br/>/Game/.../UIInfo"]
    INIT["游戏启动<br/>UIDef:InitUIDef()"]
    UDEF["UIDef.UIInfo['UI_xxx']<br/>(Lua 表)"]
    BIZ["业务代码<br/>UIManager:OpenUI(UIDef.UIInfo.UI_xxx)"]

    EDIT -->|新增 / 修改一行| DT
    DT -->|GH_FunctionLibrary.GetUIInfoContent| INIT
    INIT --> UDEF
    UDEF --> BIZ
```

3D LGUI 界面则注册到 **`LGUIInfo` DataTable**,流程一致。

## UIInfo 字段总表

```mermaid
flowchart TB
    subgraph CORE["核心标识"]
        UIName["UIName<br/>string<br/>唯一标识"]
        WCP["WidgetClassPath<br/>ObjectPath<br/>(自动拼 _C 后缀)"]
    end

    subgraph LAYER["分层与可见性"]
        UL["UILayer<br/>枚举(8 个值)"]
        FS["FullScreen<br/>bool<br/>自动隐藏下层"]
        ULI["UILayerIdent<br/>枚举(旧, 兼容)"]
    end

    subgraph INPUT["输入相关"]
        IM["IsModal<br/>bool<br/>UIMask 阻断"]
        EC["EscClose<br/>bool<br/>ESC 关闭"]
        IMC["IMC<br/>bool<br/>使用 IMC_UI 输入映射"]
        MS["Mouse<br/>枚举<br/>Show/Hide/NoEffect"]
    end

    subgraph LIFECYCLE["生命周期"]
        DO["DefaultOpen<br/>bool<br/>自动打开"]
        FO["ForceOpen<br/>bool<br/>强制打开"]
        DD["DontDestroy<br/>bool<br/>关闭时不销毁"]
        GP["Group<br/>table<br/>分组"]
    end

    subgraph AUDIO["音频"]
        OA["OpenAkEvent<br/>string"]
        CA["CloseAkEvent<br/>string"]
        AS["AudioStateType<br/>枚举"]
    end

    style UIName fill:#4a9eff,color:#fff
    style WCP fill:#4a9eff,color:#fff
    style UL fill:#51cf66,color:#fff
```

### 核心标识

| 字段 | 类型 | 说明 |
|------|------|------|
| `UIName` | string | UI 唯一标识(如 `"UI_Mail"`)。**必须唯一**,Lua 业务代码用它打开 |
| `WidgetClassPath` | ObjectPath | WBP 蓝图路径(运行时自动拼 `_C` 后缀)|

### 分层与可见性

| 字段 | 类型 | 说明 |
|------|------|------|
| `UILayer` | `Enum_UILayerNew` | 8 个值之一:`SceneLayer/SystemLayer/GuideLayer/LoadingLayer/TopLayer/TipsLayer/AlertLayer/HUDLayer`(详见 [3. UI 栈与 Layer](3.%20UI%20栈与%20Layer.md)) |
| `UILayerIdent` | `Enum_UILayer` | 旧层级枚举(兼容老代码,新 UI 用 `UILayer`) |
| `FullScreen` | bool | 是否全屏(触发自动隐藏下层) |

### 输入相关

| 字段 | 类型 | 说明 |
|------|------|------|
| `IsModal` | bool | 是否模态(自动插入 `UIMask` 阻断下层输入) |
| `EscClose` | bool | ESC 是否关闭(走 `CanEscClose → CloseTopUIByEscClose`) |
| `IMC` | bool | 是否使用 `IMC_UI` 输入映射(EnhancedInput Mapping Context) |
| `Mouse` | `Enum_UIMouseVisibility` | 鼠标行为(Show/Hide/NoEffect) |

### 生命周期与池化

| 字段 | 类型 | 说明 |
|------|------|------|
| `DefaultOpen` | bool | 是否随游戏自动打开(用于 HUD) |
| `ForceOpen` | bool | 强制打开(无视某些前置条件) |
| `DontDestroy` | bool | 关闭时不销毁,实例池化复用(详见 [3. UI 栈与 Layer](3.%20UI%20栈与%20Layer.md)) |
| `Group` | table | UI 分组(`Communication`/`Sequence` 等),用于互斥关闭 |

### 音频

| 字段 | 类型 | 说明 |
|------|------|------|
| `OpenAkEvent` | string | 打开 Wwise 事件 |
| `CloseAkEvent` | string | 关闭 Wwise 事件 |
| `AudioStateType` | `Enum_EUIAudioStateType` | 音频状态类型(由 `ui_audio_state_manager` 处理) |

## 典型场景配置组合

```mermaid
flowchart LR
    subgraph HUD["主 HUD"]
        H1["UILayer = HUDLayer<br/>FullScreen = false<br/>IsModal = false<br/>EscClose = false<br/>DontDestroy = true<br/>DefaultOpen = true"]
    end

    subgraph SYS["全屏系统界面<br/>(背包 / 角色面板)"]
        S1["UILayer = SystemLayer<br/>FullScreen = true<br/>IsModal = false<br/>EscClose = true<br/>Mouse = Show"]
    end

    subgraph ALERT["二次确认弹窗"]
        A1["UILayer = AlertLayer<br/>FullScreen = false<br/>IsModal = true<br/>EscClose = true"]
    end

    subgraph TIP["飘字 / 提示"]
        T1["UILayer = TipsLayer<br/>FullScreen = false<br/>IsModal = false<br/>EscClose = false"]
    end

    subgraph LOAD["Loading 界面"]
        L1["UILayer = LoadingLayer<br/>FullScreen = true<br/>IsModal = true<br/>EscClose = false"]
    end
```

## ui_define.lua 内部做了什么

启动时序:

```mermaid
sequenceDiagram
    participant L as ui_manager_launcher
    participant D as ui_define.lua
    participant FN as GH_FunctionLibrary
    participant DT as DataTable UIInfo

    L->>D: InitUIDef()
    D->>FN: GetUIInfoContent("UI_Mail")
    FN->>DT: 读取行
    DT-->>FN: 返回字段表
    FN-->>D: 解析为 Lua table
    D->>D: UIDef.UIInfo["UI_Mail"] = {<br/>  UIName, WidgetClassPath,<br/>  UILayer, FullScreen, ...<br/>}
    Note over D: 所有 UIInfo 行<br/>同步装入 UIDef.UIInfo 表
    L->>D: InitLGUIDef() (3D)
    D->>D: UIDef.LGUIInfo["LGUI_xxx"] = ...
```

业务代码引用方式:

```lua
local UIDef     = require('ui.uiframework.ui_define')
local UIManager = require('ui.uiframework.ui_manager')

UIManager:OpenUI(UIDef.UIInfo.UI_Mail)
-- 或动态名字:
UIManager:OpenUIByName("UI_Mail")
```

## 注册新 UI 的步骤(Cookbook 摘要)

完整流程见 [11. 新 UI Cookbook 与真实模板](11.%20新%20UI%20Cookbook%20与真实模板.md)。最关键的两步:

```mermaid
flowchart LR
    S1["① 创建 WBP<br/>实现 IUnLuaInterface<br/>GetClientModuleName 返回<br/>'ui.widget.X.ui_my_panel'"]
    S2["② 在 DataTable UIInfo<br/>新增一行<br/>填全 UIInfo 字段"]
    S3["③ 写 Lua 类<br/>继承 UIWindowBase"]
    S4["④ 业务代码<br/>UIManager:OpenUI(<br/>  UIDef.UIInfo.UI_xxx)"]
    S1 --> S2 --> S3 --> S4
    style S2 fill:#4a9eff,color:#fff
```

## 陷阱

- **UIName 不唯一**:UIManager 状态字典冲突,后开的覆盖先开的实例引用,可能导致 close 不掉
- **WidgetClassPath 拼了 `_C` 后缀**:不要拼,框架会自动拼
- **UILayer 选错**:HUD 类 UI 配成 `AlertLayer` 会一直浮在最上层挡视线
- **FullScreen + 不该全屏的 UI**:每次打开都把 HUD 隐了

[^49]: [[higame-ui-window-lifecycle|HiGame UIWindowBase 生命周期 + UIInfo 配置]] · 本地代码考古

## Sources

| # | Title | Raw Note | Original |
|---|-------|----------|----------|
| 49 | UIWindowBase 生命周期 + UIInfo 配置 | [[higame-ui-window-lifecycle]] | p4://Content/CP0032305_GH/Blueprints/DT/UIInfo |
