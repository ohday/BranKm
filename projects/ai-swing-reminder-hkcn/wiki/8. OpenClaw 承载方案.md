---
title: OpenClaw 承载方案
tags: [openclaw, vps, skill, cron, deployment]
updated: 2026-04-24
---

# OpenClaw 承载方案

策略 + 信号 + 通知层都想好了，**最后一公里**是怎么让它 7×24 在线自动跑。本页解决 **Skill 机制 + Cron 调度 + 数据持久化 + VPS 选型**。

## OpenClaw 的角色与边界

```mermaid
flowchart TB
    OC["OpenClaw<br/>自托管 7×24 agent"]

    OC --> R1["✅ 能做"]
    OC --> R2["❌ 不做"]

    R1 --> A1["Skill 注册 + 执行"]
    R1 --> A2["Cron 调度"]
    R1 --> A3["IM 通道（WhatsApp/iMessage/Slack/Signal/TG）"]
    R1 --> A4["LLM 对话 + 工具调用"]
    R1 --> A5["文件系统 / shell / 浏览器"]

    R2 --> B1["策略计算（你的 Python 脚本）"]
    R2 --> B2["回测引擎（Qlib/Backtrader）"]
    R2 --> B3["数据存储（你的 SQLite/Parquet）"]
    R2 --> B4["Futu OpenD（另起 systemd 进程）"]

    style R1 fill:#c8e6c9
    style R2 fill:#fff3cd
```

**核心理解**：OpenClaw 是**一个壳 + 调度器 + IM 出口**。真正的策略逻辑在你自己写的 Python scripts 里，OpenClaw 负责"到点调起来"+"把结果推到 IM"。

## SKILL.md 格式（本项目核心）[^31]

### 完整示例

```markdown
---
name: ai-swing-reminder
description: 扫描 watchlist 的波段信号，触发时推送提醒。关键词：盘后扫描、每日信号、波段提醒
version: 0.1.0
user-invocable: true
metadata: {"openclaw":{"requires":{"bins":["python3"],"env":["AKSHARE_OK","WECOM_WEBHOOK","TELEGRAM_BOT_TOKEN","TELEGRAM_CHAT_ID"]},"primaryEnv":"AKSHARE_OK"}}
---

# 波段提醒 Skill

## 用户调用方式
- `/ai-swing-reminder` → 立即扫描当前日
- `/ai-swing-reminder backtest --symbol 600519` → 单股票回测
- `/ai-swing-reminder update-watchlist --add 300750` → 维护池
- Cron: 每日 15:10 / 16:10 自动运行

## 步骤（扫描模式）
1. 读 watchlist: `cat {baseDir}/references/watchlist.md`
2. 扫描: `python3 {baseDir}/scripts/daily_scan.py --watchlist {baseDir}/references/watchlist.md --params {baseDir}/references/strategy-params.yaml --output /tmp/signals-$(date +%Y%m%d).json`
3. 推送: `python3 {baseDir}/scripts/notify.py --input /tmp/signals-$(date +%Y%m%d).json --channels wecom,telegram`
4. 汇报：扫描总数 / 触发数 / 推送渠道 / 耗时

## 错误处理
- 数据源失败：AkShare 失败 fallback Baostock（A 股）/ Futu OpenAPI（港股）
- 企业微信失败：Server 酱 fallback
- 失败写 /tmp/openclaw-errors-$(date +%Y%m%d).log
```

### Frontmatter 字段清单

```mermaid
flowchart LR
    FM["SKILL.md YAML frontmatter"]

    FM --> name["name · 唯一标识 + /slash"]
    FM --> desc["description · 触发匹配文本"]
    FM --> ver["version · 发布用"]
    FM --> ui["user-invocable · 暴露 /command"]
    FM --> dmi["disable-model-invocation · 禁止 LLM 上下文注入"]
    FM --> md["metadata · 单行 JSON<br/>（多行会解析坏！）"]

    md --> mdR["requires.bins · PATH 二进制"]
    md --> mdE["requires.env · 必需环境变量"]
    md --> mdP["primaryEnv · 关联 apiKey 快捷方式"]
```

**关键踩坑**：`metadata` 必须**单行 JSON**，加换行就炸。

## Skill 目录结构（规范）

```
~/.openclaw/skills/ai-swing-reminder/
├── SKILL.md                # 唯一必需文件
├── scripts/                # 被执行（不读入 context）
│   ├── daily_scan.py
│   ├── backtest_strategy.py
│   ├── walk_forward.py
│   └── notify.py
├── references/             # 按需被 agent 读入 context
│   ├── watchlist.md
│   ├── strategy-params.yaml
│   └── prompt-templates.md
└── assets/                 # 不入 context（模板/静态）
    └── signal-template.md
```

### 三种目录的区别

```mermaid
flowchart LR
    subgraph Scripts ["scripts/"]
        S1["可执行 Python / Shell"]
        S2["SKILL.md 用 {baseDir}/scripts/... 引用"]
        S3["需要 chmod +x"]
        S4["❌ 不读入 LLM context"]
    end

    subgraph Refs ["references/"]
        R1["Markdown / YAML / 配置"]
        R2["SKILL.md 指示 agent 读"]
        R3["✅ 读入 LLM context"]
    end

    subgraph Assets ["assets/"]
        A1["模板 / 静态数据"]
        A2["脚本内部读取"]
        A3["❌ 不读入 LLM context"]
    end
```

## Skill 加载优先级

```mermaid
flowchart TB
    L1["① workspace/skills/<br/>（最高优先级，项目专属）"]
    L2["② ~/.openclaw/skills/<br/>（跨 agent 共享）"]
    L3["③ Bundled skills<br/>（随安装）"]
    L4["skills.load.extraDirs<br/>（团队共享）"]

    L1 -.overrides.-> L2
    L2 -.overrides.-> L3
```

## Secrets 管理

```mermaid
flowchart LR
    subgraph M1 ["方式 1 · openclaw.json"]
        O1["skills.entries.<name>.env<br/>{TUSHARE_TOKEN: xxx}"]
    end

    subgraph M2 ["方式 2 · .env 文件"]
        O2["~/.env 或 workspace/.env"]
    end

    subgraph M3 ["Gate 机制"]
        O3["requires.env<br/>变量缺失 → skill 不加载"]
    end

    Skill --> M1
    Skill --> M2
    Skill -.保护性检查.-> M3
```

**openclaw.json 示例**：
```json
{
  "skills": {
    "entries": {
      "ai-swing-reminder": {
        "enabled": true,
        "env": {
          "TUSHARE_TOKEN": "your-token",
          "TELEGRAM_BOT_TOKEN": "123:ABC",
          "WECOM_WEBHOOK": "https://qyapi.weixin.qq.com/..."
        }
      }
    }
  }
}
```

## Cron 调度引擎[^31]

### 启用内置 cron skill

```bash
claw skill enable cron-job
# 或
clawhub install cron-job
```

### 添加任务

```bash
# A 股盘后扫描
openclaw cron add \
  --name "a-share-daily-scan" \
  --cron "10 15 * * 1-5" \
  --channel wecom,telegram \
  --message "/ai-swing-reminder market=a-share"

# 港股盘后扫描
openclaw cron add \
  --name "hk-daily-scan" \
  --cron "10 16 * * 1-5" \
  --message "/ai-swing-reminder market=hk"

# 周末研报
openclaw cron add \
  --name "weekend-research" \
  --cron "0 10 * * 6" \
  --message "/tradingagents-cn-watchlist-review"

# 每日心跳
openclaw cron add \
  --name "daily-heartbeat" \
  --cron "59 23 * * *" \
  --message "/heartbeat"
```

### Cron 存储

```
~/.openclaw/cron/
├── jobs.json                        # 任务定义
└── runs/
    ├── a-share-daily-scan.jsonl     # 执行日志
    ├── hk-daily-scan.jsonl
    └── ...
```

**日志排错**：`jsonl` 格式，每行一条执行记录（时间戳 + 状态 + 输出）。

### 独立会话 vs 主会话

```mermaid
flowchart LR
    subgraph Indep ["独立会话"]
        I1["无上下文污染"]
        I2["脚本型任务首选"]
        I3["调用一次结束"]
    end

    subgraph Main ["主会话"]
        M1["共享历史"]
        M2["连续对话型"]
        M3["适合 '提醒我昨天说了什么'"]
    end

    Cron["Cron 触发"] -.选一.-> Indep
    Cron -.选一.-> Main

    Scan["策略扫描（本项目）"] -.推荐.-> Indep
    Chat["日常提问"] -.推荐.-> Main
```

**本项目所有 cron 用独立会话**——脚本跑完就退，不污染对话。

## 数据持久化选型[^31]

### 需求分解

```mermaid
flowchart LR
    Need["存储需求"] --> N1["历史日 K<br/>A 股 5000 股 × 10 年 ≈ 18M 行"]
    Need --> N2["触发日志<br/>高频写入小粒度"]
    Need --> N3["策略状态<br/>KV 查找"]
    Need --> N4["回测结果缓存<br/>列式聚合"]
```

### 三方案对比

| 维度 | **SQLite** | **Parquet (pyarrow)** | **DuckDB** |
|---|---|---|---|
| 安装 | Python 自带 | `pip install pyarrow` | `pip install duckdb` |
| 架构 | 行式，文件级 | 列式，文件级 | 列式，进程内 |
| 事务 | ✅ ACID | ❌ | ✅ |
| 频繁写入 | ✅ | ❌（append 需 rewrite） | ✅ |
| 分析性能 | 🟡 | ✅✅ | ✅✅✅ |
| 时序 rolling | 🟡 | 依赖 Python | ✅ 原生 |
| 压缩率 | 中 | **最优** | 读 Parquet |
| 适合规模 | GB 级 | TB 级 | TB 级 |

### 混合架构（本项目推荐）

```mermaid
flowchart TB
    subgraph SQLite ["事务/状态"]
        S1["watchlist.db<br/>监控列表"]
        S2["signals.db<br/>触发日志 + 去重"]
        S3["strategy_state.db<br/>KV"]
        S4["openclaw_jobs.db<br/>任务历史"]
    end

    subgraph Parquet ["历史数据（磁盘）"]
        P1["data/a_share_daily/year=2025/part-01.parquet"]
        P2["data/hk_daily/year=2025/part-01.parquet"]
        P3["data/factor_cache/strategy=ma20/..."]
    end

    subgraph DuckDB ["分析查询"]
        D1["duckdb.sql('SELECT * FROM read_parquet(...)')"]
    end

    Parquet -.挂载查询.-> DuckDB
    SQLite -.策略状态.-> Script
    DuckDB -.历史分析.-> Script
    Script["策略脚本"]
```

**核心理念**：**事务 用 SQLite，历史 用 Parquet，分析 用 DuckDB 读 Parquet**。

## VPS 选型（关键！）[^31]

### 数据源与 IP 地域约束

```mermaid
flowchart LR
    subgraph CN_needs ["需要大陆 IP"]
        A["爬东财 / 新浪 (AkShare)"]
        B["Baostock（国内服务器）"]
    end

    subgraph HK_needs ["香港/海外 IP 更优"]
        C["Futu OpenAPI（延迟 1-3ms）"]
        D["Longbridge"]
        E["Telegram (墙)"]
        F["OpenAI/Anthropic API (墙)"]
    end

    subgraph Dual ["双向可达"]
        G["Tushare Pro（HTTPS）"]
        H["DeepSeek / Qwen API"]
        I["企业微信 webhook"]
    end
```

### 单节点 vs 双节点

**方案 A · 单海外节点（推荐）**
- 腾讯云/阿里云香港轻量（4核4G, $12/月）
- AkShare 爬国内稍延迟但日频够用
- Futu 就近接入
- Telegram / OpenAI 直通
- **月开销 ≈ $12**

**方案 B · 双节点（生产进阶）**
- 节点 1 大陆：Tushare / AkShare / Baostock 爬取 + 数据分析
- 节点 2 香港：OpenClaw + Futu + Telegram + LLM
- Tailscale / WireGuard 打通，每晚同步数据
- **月开销 ≈ $12 (HK) + ¥30 (大陆) = ¥110**

```mermaid
flowchart LR
    subgraph Single ["方案 A · 单节点（推荐起步）"]
        HK["腾讯云香港 4核4G<br/>$12/月"]
        HK --> All["OpenClaw + 数据 + 策略 + Futu OpenD"]
    end

    subgraph Double ["方案 B · 双节点（规模化再拆）"]
        CN2["大陆轻量<br/>¥30/月"]
        HK2["香港轻量<br/>$12/月"]
        CN2 -.Tailscale 每晚同步.-> HK2
    end

    Decision{"启动阶段"} -->|< 100 笔/月| Single
    Decision -->|> 100 笔/月 且多策略| Double

    style Single fill:#c8e6c9
```

### 云服务商对比

| 云 | 香港 | 大陆→香港延迟 | 香港→Futu 延迟 | 入门价 |
|---|---|---|---|---|
| **腾讯云轻量香港** | ✅ | 20-40ms | **1-3ms**（富途合作） | $12/月 |
| 阿里云香港 | ✅ | 30-50ms (CN2) | 5-10ms | $15/月 |
| Vultr HK | ✅ | 50-80ms | 5-15ms | $6/月 |
| Digital Ocean SG | ✅ | 80-120ms | 10-20ms | $6/月 |

**推荐**：腾讯云轻量香港（和富途合作，延迟最低）。

## Futu OpenD 部署

`OpenD` 必须 **7×24 在线**——systemd 是标准做法：

```ini
# /etc/systemd/system/futu-opend.service
[Unit]
Description=Futu OpenD
After=network.target

[Service]
Type=simple
User=openclaw
ExecStart=/opt/OpenD/OpenD \
    --login_account=xxx \
    --login_pwd=xxx \
    --lang=en \
    --console_log=all
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable --now futu-opend
sudo systemctl status futu-opend
journalctl -u futu-opend -f  # 看日志
```

## 完整架构图（本项目生产部署）

```mermaid
flowchart TB
    subgraph VPS ["腾讯云香港轻量 4核4G 8G（推荐升内存） · $12-20/月"]

        subgraph OC_domain ["OpenClaw 主进程"]
            CL["OpenClaw daemon"]
            subgraph Skills ["Skills"]
                SK1["ai-swing-reminder（本项目）"]
                SK2["cron-job（内置）"]
                SK3["tradingagents-cn-wrapper"]
                SK4["brave-search（可选）"]
            end
            subgraph Jobs ["Cron"]
                J1["08:50 盘前"]
                J2["15:10 A 盘后"]
                J3["16:10 HK 盘后"]
                J4["10:00 Sat 研报"]
                J5["23:59 心跳"]
            end
            subgraph Storage ["本地存储"]
                DB1[("SQLite<br/>signals/state")]
                DB2[("Parquet<br/>历史 K")]
                DB3[("DuckDB<br/>分析")]
            end
        end

        subgraph Side ["Sidecar 进程"]
            OpenD["Futu OpenD<br/>systemd"]
            TACN["TradingAgents-CN<br/>docker compose"]
        end

    end

    subgraph Out ["出口"]
        WC["企业微信"]
        TG["Telegram"]
        BK["Bark"]
        Futu["富途交易"]
    end

    subgraph In ["数据源"]
        AK["AkShare"]
        BS["Baostock"]
        TS["Tushare"]
        FT["Futu OpenAPI"]
        LLM["DeepSeek / Claude API"]
    end

    In --> CL
    CL --> Skills
    CL --> Jobs
    Skills --> Storage
    Skills --> OpenD
    Skills --> TACN
    Skills --> Out
```

## 踩坑清单（生产经验）

```mermaid
flowchart TB
    subgraph T1 ["Skill 层"]
        P1["metadata JSON 换行 → 解析坏"]
        P2["scripts/*.py 忘 chmod +x"]
        P3["requires.env 变量缺 → skill 不加载（排查看 openclaw logs）"]
    end

    subgraph T2 ["Cron 层"]
        P4["时区不对 → cron 误触"]
        P5["jobs.jsonl 静默失败 → 心跳任务必建"]
        P6["独立会话 vs 主会话选错"]
    end

    subgraph T3 ["VPS 层"]
        P7["Futu OpenD 断线 → systemd Restart=always"]
        P8["腾讯云轻量带宽配额触顶 → 升带宽"]
        P9["4G 内存跑不动 TradingAgents-CN → 升 8G"]
    end

    subgraph T4 ["推送层"]
        P10["推送超 4096 字节 → 自动分页"]
        P11["Telegram 在大陆直连 → 必须放海外节点"]
        P12["企业微信 45009 限频 → 指数退避"]
    end
```

## 热重载 & 快速迭代

`skills.load.watch` 开启后，**Skill 文件改动立即生效**（下一个 agent turn）。调试效率大幅提升。

## 下一步

承载方案定完后就是**港股下单**——见 [9. 合规红线与港股下单 API](9.%20合规红线与港股下单%20API.md)。

---

[^31]: [[openclaw-hosting-architecture|OpenClaw 承载方案]] · 综合自 [OpenClaw Skill 教程](https://2048ai.net/69a28da454b52172bc5e1758.html) · [OpenClaw 实战指南 CSDN](https://blog.csdn.net/quyixiao/article/details/160382493) · [定时任务全攻略 阿里云](https://developer.aliyun.com/article/1718611) · [openclaw.ai](https://openclaw.ai/)

## Sources

| # | Title | Raw Note |
|---|-------|----------|
| 31 | OpenClaw 承载方案 | [[openclaw-hosting-architecture]] |
