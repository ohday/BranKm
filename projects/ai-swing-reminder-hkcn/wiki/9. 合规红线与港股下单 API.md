---
title: 合规红线与港股下单 API
tags: [compliance, a-share, hong-kong, futu, longbridge, tiger, regulation]
updated: 2026-04-24
---

# 合规红线与港股下单 API

本页分两个 part：**①A 股为什么只能提醒不能下单**（2024 新规的合规红线），**②港股下单 API 三家对比**（Futu / Longbridge / Tiger）+ **半自动工作流**。

## Part 1 · A 股合规红线

### 法规时间线

```mermaid
timeline
    title A 股程序化交易监管演进
    2023-09-01 : 上交所首次报告制
    2024-05-15 : 证监会《程序化交易管理规定（试行）》
    2024-06-10 : 沪深北交易所实施细则
    2024-07-11 : 异常交易『红线』划定
    2024-10-08 : 新规正式实施
```

**源**：证监会 2024 年 8 号公告 + 国务院公报[^32]

### 定义：程序化交易 = 计算机程序自动生成或执行交易指令

**关键澄清**：
- 个人投资者**同等适用**（很多人以为只管私募）
- 高频认定：申报 **≥ 300 笔/秒** 或 单日 **≥ 20000 笔**（本项目日频不触发）
- "先报告后交易"原则 —— 未报备直接跑策略 = 违规

```mermaid
flowchart TB
    Ops["操作方式"]

    Ops --> NR["纯提醒 · 人工下单<br/>❌ 不算程序化"]
    Ops --> SA["半自动 · API 下单"]
    Ops --> FA["全自动 · API 下单"]

    NR --> N_OK["✅ 合规<br/>本项目 A 股选这条"]
    SA --> SA_Check{"是否向券商报备？"}
    FA --> FA_Check{"是否向券商报备？"}

    SA_Check -->|未报备| SA_Bad["🔴 违规"]
    SA_Check -->|已报备| SA_OK["✅ 合规<br/>（需 QMT 50 万门槛）"]
    FA_Check -->|已报备 + 低频| FA_OK["✅ 合规"]
    FA_Check -->|未报备 或 >300 笔/秒| FA_Bad["🔴 违规"]

    style N_OK fill:#c8e6c9
    style SA_OK fill:#c8e6c9
    style FA_OK fill:#c8e6c9
    style SA_Bad fill:#ffcdd2
    style FA_Bad fill:#ffcdd2
```

### 异常交易红线（禁止行为）[^32]

```mermaid
flowchart TB
    R["红线 3 条"]
    R --> R1["① 虚假挂单<br/>频繁申报后撤单<br/>撤单率异常"]
    R --> R2["② 操纵市场<br/>拉抬打压 · 影响收盘价<br/>制造『秒板』"]
    R --> R3["③ 算法共振<br/>短时间大额成交<br/>造成市场波动"]

    style R1 fill:#ffcdd2
    style R2 fill:#ffcdd2
    style R3 fill:#ffcdd2
```

### 本项目为什么推荐"纯提醒"

```mermaid
flowchart LR
    System["系统输出"] -->|"『建议买入 600519』"| User["自然人"]
    User -->|"手动打开券商 APP 下单"| Exchange["交易所"]

    subgraph Analysis ["合规分析"]
        A1["系统输出是『分析报告』"]
        A2["不是『交易指令』"]
        A3["上交所明确：<br/>提供投资建议不属于程序化交易"]
    end

    style User fill:#c8e6c9
```

**原则**：
> **对自己操作给出建议从古至今都合法。代替别人做决定需要持牌。让机器代替自己做决定需要报备。本项目是第一种。**

### A 股进阶合规路径（未来扩展）

如果账户持仓 ≥ 50 万，可开通券商 miniQMT（`xtquant`）：

```mermaid
flowchart LR
    Prep["前置条件"] --> P1["账户资产 ≥ 50 万<br/>（部分券商 30 万）"]
    Prep --> P2["向券商报备策略大类"]
    Prep --> P3["签署程序化交易风险揭示书"]

    Prep --> Dep["部署"] --> D1["xtquant Python SDK"] --> D2["本地 MiniQMT 客户端"]

    Deploy --> Constraints["约束"]
    Constraints --> C1["不做高频<br/>≤ 300 笔/秒<br/>≤ 20000 笔/日"]
    Constraints --> C2["不做虚假挂单"]
    Constraints --> C3["关注交易所红线"]
```

**各方案门槛**：

| 方案 | 资金门槛 | API | 适用 |
|---|---|---|---|
| xtquant / miniQMT | 50 万 | Python，免费 | 散户合法路径 |
| Ptrade（恒生） | 100 万 | Python/VBA | 中高净值 |
| 机构柜台（O32/金证） | 500 万 + 私募资质 | C++/FIX | 机构 |
| easytrader GUI 模拟 | 无门槛 | GUI 模拟 | **🔴 违规** |

### easytrader 不推荐的原因

```mermaid
flowchart TB
    ET["easytrader<br/>GUI 键鼠模拟"]

    ET --> Tech["技术上可行"]
    ET --> Reg["新规下属违规"]
    ET --> Anti["券商反欺诈"]

    Tech --> T1["模拟同花顺窗口<br/>自动点击下单"]

    Reg --> R1["本质是程序化交易"]
    Reg --> R2["未走报备通道"]
    Reg --> R3["不符 2024-10-08 新规"]

    Anti --> A1["券商检测窗口标题"]
    Anti --> A2["识别异常 click 模式"]
    Anti --> A3["封账号 + 列入黑名单"]

    style ET fill:#ffcdd2
```

## Part 2 · 港股下单 API 全维度对比

### 三家基本盘

```mermaid
flowchart TB
    HK["香港市场<br/>SFC 监管"]

    HK --> Futu["Futu OpenAPI<br/>富途证券<br/>富途香港（SFC Type 1/4/9）"]
    HK --> LB["Longbridge OpenAPI<br/>长桥证券<br/>（SFC Type 1/2/4/9）"]
    HK --> Tiger["Tiger OpenAPI<br/>老虎证券<br/>（HK + SG + US 多牌照）"]

    Futu --> F_App["futu-api"]
    LB --> L_App["longport"]
    Tiger --> T_App["tigeropen"]
```

### 全维度对比表[^32]

| 维度 | **Futu** | **Longbridge** | **Tiger** |
|---|---|---|---|
| **Python SDK** | `futu-api` | `longport` | `tigeropen` |
| **本地 daemon** | ✅ 需 **OpenD** | ❌ | ❌ |
| **市场** | HK/US/CN-A/SG/JP/AU | HK/US/CN-A/SG | HK/US/SG/AU/CN-A |
| **港股 LV2** | **✅ 大陆个人免费** | 需付费 | 需付费 |
| **开户门槛** | HKD 10000 | 无最低（建议 10000+） | USD 3000 / HKD 25000 |
| **港股佣金** | 0.03% 最低 **3 HKD** | 0.03% 最低 **3 HKD** | 0.03% 最低 **7 HKD** |
| **订单类型** | 市/限/止损/条件/特殊 HK | 市/限/到价 Stop/追踪止损 | MKT/LMT/STP/STP_LMT/Trailing |
| **架构** | TCP via OpenD | REST + WebSocket | REST + WebSocket |
| **行情限频** | ~30/s | 1s ≤ 10 次，并发 ≤ 5 | 60-120/min |
| **交易限频** | 宽松 | 30s ≤ 30 次，间隔 ≥ 0.02s | 中等 |
| **SDK 内置限流** | 🟡 部分 | 行情 ✅，交易 **❌** | 🟡 |
| **错误码** | 自定义 | 返回 `301606` | HTTP 标准 |
| **社区** | ✅ 最大 | 🟡 中等 | 🟡 中等 |
| **下单额外费** | ✅ 无 | ✅ 无 | ✅ 无 |

### 决策树

```mermaid
flowchart TD
    Start["开通港股下单 API？"]
    Start --> Q1{"已有账户吗？"}
    Q1 -->|富途/moomoo| F_direct["✅ Futu OpenAPI"]
    Q1 -->|长桥| L_direct["✅ Longbridge"]
    Q1 -->|老虎| T_direct["✅ Tiger"]
    Q1 -->|全新| Q2{"首要诉求？"}

    Q2 -->|省钱省事| Q3["✅ Futu<br/>LV2 免费 + 最低佣金 3 HKD"]
    Q2 -->|轻量部署| Q4["✅ Longbridge<br/>无本地 daemon"]
    Q2 -->|已用美股 Tiger| Q5["✅ Tiger 多市场统一"]

    style Q3 fill:#c8e6c9
    style Q4 fill:#e3f2fd
```

### Longbridge 完整代码示例[^32]

**限价买入**：
```python
from longbridge.openapi import TradeContext, Config, OrderSide, OrderType, TimeInForceType

config = Config.from_env()   # 读 LONGPORT_APP_KEY / SECRET / ACCESS_TOKEN
ctx = TradeContext(config)

ctx.submit_order(
    symbol="0700.HK",
    order_type=OrderType.Limit,
    side=OrderSide.Buy,
    submitted_price=300.0,
    submitted_quantity=100,
    time_in_force=TimeInForceType.Day,
)
```

**到价触发的限价单（条件单）**：
```python
ctx.submit_order(
    symbol="0700.HK",
    order_type=OrderType.Limit,
    side=OrderSide.Buy,
    submitted_price=310.0,
    submitted_quantity=100,
    time_in_force=TimeInForceType.Day,
    trigger_price=310.0,
    trigger_at="last"
)
```

### Longbridge 限流陷阱（踩坑必读）

```mermaid
flowchart LR
    Code["你的代码"] -->|"for order in orders:<br/>ctx.submit_order(...)"| Burst["爆发 100 单"]
    Burst -->|"SDK 未内置交易限流"| Server["长桥服务器"]
    Server -->|"30s ≤ 30 次<br/>两次间隔 ≥ 0.02s"| Err["错误码 301606"]

    Fix["修复"] --> Fix1["for order in orders:<br/>ctx.submit_order(...)<br/>time.sleep(0.05)"]

    style Err fill:#ffcdd2
    style Fix1 fill:#c8e6c9
```

多子账户需**独立计频**（每账户单独 counter）。

### Futu OpenD 部署注意（已在 [8. OpenClaw 承载方案](8.%20OpenClaw%20承载方案.md) 详述）

关键点重申：
- OpenD **必须 7×24 在线**
- systemd 配置 `Restart=always`
- 富途服务器定期维护会断 OpenD，代码要处理重连

## Part 3 · 半自动工作流（提醒 → 确认 → 下单）

### 完整 Flow

```mermaid
sequenceDiagram
    participant Scan as OpenClaw 扫描<br/>15:10 cron
    participant IM as Telegram
    participant U as 用户
    participant OC as OpenClaw
    participant Futu as Futu OpenAPI

    Scan->>IM: 📈 0700.HK 突破信号<br/>建议买 100 股 @ 315<br/>[⚠️ 30 分钟内有效]<br/>/tc_BUY_0700_100_315 / /tc_IGNORE

    U->>IM: 查看信号
    Note over U: 人工判断：<br/>基本面？仓位？风险？

    alt 场景 A · 手动下单
        U->>U: 打开富途 App 手动下单
    else 场景 B · 指令下单
        U->>IM: /tc_BUY_0700_100_315
        IM->>OC: 解析指令
        OC->>OC: 匹配 signal_id<br/>检查有效期 (< 30 min)
        OC->>IM: ⚠️ 确认 BUY 100 股 0700.HK @ 315?<br/>回复 YES
        U->>IM: YES
        IM->>OC: 二次确认
        OC->>Futu: place_order(...)
        Futu-->>OC: order_id = xxx
        OC->>IM: ✅ 订单 xxx 已提交

        Note over OC,Futu: 5-10s 后拉成交
        OC->>Futu: query_order(xxx)
        Futu-->>OC: filled 100@315.20
        OC->>IM: ✅ 部分成交 100 @ 315.20
    end
```

### 为什么一定要人工确认这一步

```mermaid
flowchart TB
    AI["让 AI 自动下单<br/>的风险"]

    AI --> R1["🔴 LLM 幻觉"]
    AI --> R2["🔴 价格漂移<br/>（盘后信号到开盘差 2-3%）"]
    AI --> R3["🔴 资金管理疏漏<br/>（AI 不知道你其他账户）"]
    AI --> R4["🔴 合规心理<br/>（『不是我自己决定的』）"]
    AI --> R5["🔴 算法迷信<br/>（长期全自动失去反思能力）"]

    Human["人工确认<br/>解决 5 条"]
    Human --> S1["✅ 识别明显错判"]
    Human --> S2["✅ 当日价格复核"]
    Human --> S3["✅ 组合仓位检查"]
    Human --> S4["✅ 主动同意 = 自己负责"]
    Human --> S5["✅ 保持判断力"]
```

### `/tc_*` 指令解析代码

```python
# skill 里的 message handler
@skill_handler(r"^/tc_")
def on_trade_confirm(cmd: str, ctx: ChatContext):
    # "/tc_BUY_0700.HK_100_315"
    parts = cmd[4:].split("_")
    if len(parts) != 4:
        return "❌ 指令格式错误"
    action, symbol, qty_str, price_str = parts
    qty, price = int(qty_str), float(price_str)

    # 有效期检查（30 分钟）
    signal = load_signal(symbol)
    if not signal:
        return "❌ 找不到对应信号"
    if (now() - signal.ts).total_seconds() > 1800:
        return "⏱️ 信号已过期（30 分钟），请重新扫描"

    # 二次确认
    ctx.reply(f"⚠️ 确认 {action} {qty} {symbol} @ {price}？\n回复 YES 提交")
    reply = ctx.await_reply(timeout=60)
    if reply != "YES":
        return "已取消"

    # 现金与持仓预检
    account = futu_client.get_account_info()
    if action == "BUY" and account.cash < qty * price:
        return f"❌ 现金不足（{account.cash:.0f} < {qty * price:.0f}）"

    # 下单
    try:
        order = futu_client.place_order(
            symbol=symbol, action=action, qty=qty,
            price=price, order_type="NORMAL", time_in_force="DAY"
        )
        return f"✅ 订单 {order.order_id} 已提交"
    except FutuApiError as e:
        return f"❌ 下单失败：{e}"
```

## 避坑清单（中国大陆投资者跨境操作）

```mermaid
flowchart TB
    subgraph CN_HK ["大陆居民开港股账户"]
        K1["✅ 合法：通过港资券商（Futu HK / 长桥 / 老虎）"]
        K2["✅ 合法：按规定申报海外证券账户个税（全球征税）"]
        K3["⚠️ 注意：5 万美元/年购汇额度限制"]
        K4["⚠️ 注意：W-8BEN 美股预提税（仅美股账户）"]
    end

    subgraph Dont ["🔴 不要做"]
        D1["easytrader GUI 下单 A 股"]
        D2["未报备的程序化大资金 A 股策略"]
        D3["A 股高频策略（>300 笔/秒）"]
        D4["对外提供『AI 荐股』SaaS（未持牌）"]
    end

    style K1 fill:#c8e6c9
    style K2 fill:#c8e6c9
    style D1 fill:#ffcdd2
    style D2 fill:#ffcdd2
    style D3 fill:#ffcdd2
    style D4 fill:#ffcdd2
```

## 港股 API 选型最终建议

```mermaid
flowchart LR
    subgraph Tier1 ["首选"]
        F["Futu<br/>✅ LV2 免费<br/>✅ 社区最大<br/>✅ 佣金最低<br/>⚠️ 需 OpenD 7×24"]
    end

    subgraph Tier2 ["次选"]
        L["Longbridge<br/>✅ 无 daemon<br/>✅ SDK 现代<br/>❌ LV2 付费"]
    end

    subgraph Tier3 ["已有账号再选"]
        T["Tiger<br/>✅ 多市场统一<br/>❌ 港股佣金贵 7 HKD"]
    end

    Start["选哪家？"] --> Tier1
    Start -.已有账号.-> Tier2
    Start -.已有美股 Tiger.-> Tier3

    style F fill:#c8e6c9
    style L fill:#e3f2fd
```

## 下一步

7 个关键子系统讲完，收尾看**端到端的完整 pipeline 模板** —— [10. 端到端 Pipeline 模板](10.%20端到端%20Pipeline%20模板.md)。

---

[^32]: [[compliance-and-hk-trading-api|合规红线与港股下单 API]] · 综合自 [证监会《程序化交易管理规定》](https://www.csrc.gov.cn/csrc/c100028/c7480577/content.shtml) · [国务院公报](https://www.gov.cn/gongbao/2024/issue_11426/202406/content_6959680.html) · [证监会异常交易红线](https://mrdx.cn/h5/mrdx/content/20240711/Articel05006GN.htm) · [Longbridge 官方文档](https://open.longportapp.cn/zh-CN/docs/index) · [Futu OpenAPI](https://openapi.futunn.com/) · [Tiger OpenAPI Python SDK](https://github.com/tigerfintech/openapi-python-sdk)

## Sources

| # | Title | Raw Note |
|---|-------|----------|
| 32 | 合规红线与港股下单 API | [[compliance-and-hk-trading-api]] |
